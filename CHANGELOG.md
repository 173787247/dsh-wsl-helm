# Changelog

## 0.1.1

- `helm_status`: primary read-only release status tool (`helm_release_status` kept as alias).
- Richer `helm_status_tool`: short version + filtered `helm env` snippet.

## 0.1.0

- Read-only `helm_status_tool`, `helm_list`, `helm_release_status`, `helm_history`.
